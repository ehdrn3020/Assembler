MAIN SEGMENT
     ASSUME CS:MAIN, DS:MAIN
	MOV AX, CS
	MOV DS, AX

	MOV AX,0

	LEA SI, T_A
	LEA DI, T_B

LOOP1:	MOV BX, [SI]
	MOV CX, [DI]
	CMP BX, CX
	JB CALL RETUR
L2:	INC SI
	INC SI
	INC DI
	INC DI
	INC LEN
	CMP LEN, 3
	JB LOOP1
	MOV COUNT,AX
	MOV AH, 4CH
	INT 21H
	
RETUR PROC
	INC AX
	MOV [SI],CX
	MOV [DI],BX
	JMP L2
RETUR ENDP	


T_A DW 112,5,50
T_B DW 130,20,10
LEN DW 0
COUNT DW ?

MAIN ENDS
     END
